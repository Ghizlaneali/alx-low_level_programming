#include "main.h"

/**
 * _islower - Chechs for characters in lowercase.
 *@c: the character to check
 * Return: 1 if c is lowercase, 0 if oherwise.
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
