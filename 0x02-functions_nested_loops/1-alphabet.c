#include "main.h"

/**
*print_alphabet - Print the alphabet in lowercase, followed by a new line.
*
*Return: void.
*/

void print_alphabet(void)

int main(void)
{
	char ch = a;

	for (ch = a; ch <= 'z';ch++;)
	{	
		_putchar(ch);
	}
	_putchar('\n');
}

