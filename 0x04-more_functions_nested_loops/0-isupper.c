#include "main.h"

/**
 * _islower - Chechs for characters in uppercase.
 *@c: the character to check
 * Return: 1 if c is uppercase, 0 if oherwise.
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
